import 'package:fusteka_icecreem/core/hooks/hooks.dart';

class MekanoGiftListScreen extends StatelessWidget {
  const MekanoGiftListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const MekanoGiftListBody();
  }
}
