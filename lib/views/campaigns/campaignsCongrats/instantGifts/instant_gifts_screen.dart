import 'package:fusteka_icecreem/core/hooks/hooks.dart';

class InstantGiftsScreen extends StatelessWidget {
  const InstantGiftsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const InstantGiftsBody();
  }
}
