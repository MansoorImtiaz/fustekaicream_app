import 'package:fusteka_icecreem/core/hooks/hooks.dart';

class NavMainScreen extends StatelessWidget {
  const NavMainScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const NavMainBody();
  }
}
